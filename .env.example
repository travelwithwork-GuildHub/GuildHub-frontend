# GuildHub 前端的環境變數。規格 FE-O09。
#
# 這份是**範本**，進版控。實際的值放 `.env.local`（已在 .gitignore 裡）。
#
# ⚠️ **`NEXT_PUBLIC_*` 的值會被編進 client bundle**，任何人打開 DevTools
# 都看得到。所以這裡**只能放位址，不能放任何密鑰**。這不是遺漏，
# 是這個前綴的定義（Next.js 在建置時把它們靜態替換成字面值）。
#
# ⚠️ 漏掉 `NEXT_PUBLIC_` 前綴的變數在 client bundle 裡會被替換成**空字串**，
# 不是 undefined —— 所以打錯前綴的症狀是「位址是空的」，不是「變數不存在」。
#
# 讀取全部集中在 `src/config/env.ts`，別處有 lint 規則擋著。

# ─────────────────────────────────────────────────────────────────────
# 部署目標。**不是 NODE_ENV** —— 那講的是建置模式，preview 站也是 production。
#
#   local       本機開發。下面兩個位址可以留空，會用本機預設值
#   preview     預覽站。下面兩個位址**必填**
#   production  正式站。下面兩個位址**必填**
#
# 缺席時：`next dev` 視為 local；**`next build` 出來的東西會拒絕啟動** ——
# 少了這一層，漏設代號的正式站會合法地退回 localhost，
# 而症狀是「所有資料都不見了」，不是「設定錯了」。
#
# 打錯字（例如 production 打成 prod）會拋錯，**不會退回 local**。
NEXT_PUBLIC_APP_ENV=local

# ─────────────────────────────────────────────────────────────────────
# 後端 REST 的 base URL。協定只接受 http: / https:。
#
# local 的預設值是 http://localhost:8000 —— 那是後端 `bash run.sh` 起來的位址。
# **不要填任何共用實例**：一次 40 連線的壓測足以把同時在用的人全部踢下線
#（見 AGENTS.md〈測試環境隔離〉）。
NEXT_PUBLIC_GUILDHUB_REST=

# 後端 WebSocket 的 URL。協定只接受 ws: / wss:。
#
# local 的預設值是 ws://localhost:8000/ws。
# **只填 base，不要帶查詢參數** —— `scene` 與 `token` 由連線時組上去（FE-R01）。
NEXT_PUBLIC_GUILDHUB_WS=
