/**
 * 一按即送的快捷狀態（`FE-K05`，design D5）。**產品常數，不進契約、不進規格**（`config.yaml` 的文案規則）——
 * 改字是 `chore/`。每一句都在 12 字以內（`LIMITS.statusText.max`）。
 */
export const QUICK_STATUSES = ['趕工中', '找人聊聊', '開會中', '休息一下'] as const
