## 1. 規格

- [x] 1.1 規格已在 PR 上談定（`spec/fe-w03-render-interpolation` 合併進 `main`）。
      驗證：`npx --no-install openspec validate fe-w03-render-interpolation --strict` 通過且 PR 已合併

## 2. 累加器 ＋ 插值的純函式

- [x] 2.1 新增模組：持有累加器、前一個與目前的物理位置，
      每幀吃 `dt` 與一個「跑一個固定步並回傳新位置」的回呼，
      回傳畫面位置。**回呼是唯一被替換的東西**（design 的 D6）
- [x] 2.2 沿用既有的 `planSteps`，**不要重寫一份**
- [x] 2.3 `FE-W03-S15`：畫面位置 = `v·T − v·h`。用代數推導的那條恆等式當斷言

## 3. 速率恆定（這是驗收的核心）

- [x] 3.1 `FE-W03-S14`：餵一串刻意不整除 `fixedStep` 的 `dt`
      （比一步短、比一步長、剛好一步），斷言**每一幀**的
      `畫面位移 / dt` 等於 `MOVE_SPEED`
- [x] 3.2 **不要只斷言總和** —— 目前的實作總距離就是對的
- [x] 3.3 驗證方式 V2（**驗收條件**）：把插值換成「直接回傳目前的物理位置」，
      3.1 必須變紅。紅了才算數

## 4. 靜止

- [x] 4.1 `FE-W03-S16`：沒有按鍵時畫面位置每一幀都與前一幀相同。
      驗證：連續推進若干幀，兩兩相等

## 5. 接進 LocalPlayer

- [ ] 5.1 render loop 改成呼叫 2.1 那個模組，回呼裡做
      `movePlayer(pw, delta)` 再讀 `translation()`
- [ ] 5.2 `FE-W03-S17`：`targetRef`（相機）寫**畫面位置**，
      `poseRef`（網路）寫**物理位置**，而且在非整數倍的那一幀**兩者不相等**
- [ ] 5.3 走路動畫的相位**維持用 raw dt**（design 的 D3）——
      改成固定步會把剛修好的問題搬到腳上

## 6. 在真瀏覽器上量一次

- [ ] 6.1 用 `tests/e2e/` 既有的做法（`addInitScript` ＋ `__THREE_DEVTOOLS__`）
      重跑一次逐幀位移，把修好之後的分布貼進 PR。
      **這是證據不是閘門** —— 判準是 3.1，理由見 design 的 D5
