# `FE-B01` 任務

## 1. 規格

- [x] 1.1 規格已在 PR 上談定（`spec/fe-b01-list-container`）
- [ ] 1.2 `docs/WBS.md` 的 `governance/` PR：修正「offset 翻頁」的敘述，
      並新增「資料驅動的世界看板摘要」一列（`D5` 的缺口）

## 2. 資料存取（`src/api/`）

對應 Requirement〈翻頁只用契約真正提供的參數〉

- [x] 2.1 `listProfiles({ page })` 與 `listProjects({ page })`
      —— **元件裡不准出現 `fetch`**
- [x] 2.2 判準：`S04`（第一頁送 `page=0`）、`S05`（前進送 `page=1`）

## 3. 翻頁的狀態機

對應 Requirement〈「還有沒有下一頁」只能靠實際取到的資料判定〉、
〈請求失敗 SHALL NOT 被當成翻到底〉

- [x] 3.1 三種邊界：不滿一頁／正好一頁／前進之後撲空
- [x] 3.2 判準：`S07`、`S08`、`S09`
- [x] 3.3 失敗與到底分得開；失敗之後可重試同一頁
- [x] 3.4 判準：`S10`、`S11`
- [x] 3.5 **突變**：把「正好 20 筆 → 確定還有下一頁」寫回去，`S09` 要紅

## 4. 競態

對應 Requirement〈晚到的回應不得覆蓋畫面〉

- [x] 4.1 request identity = `{ 資料種類, page }`，提交前比對
- [x] 4.2 判準：`S14`（晚到的舊頁不覆蓋新頁）、`S15`（換種類不混入）
- [x] 4.3 **突變**：把 identity 縮成只有 `page`，`S15` 要紅
      （⚠️ 人才流程會完全正常 —— 這正是它難發現的原因）

## 5. 容器與插槽

對應 Requirement〈案件與人才共用同一個容器〉、〈只做狀態，不做文案〉

- [x] 5.1 一份版型＋列表＋翻頁，資料種類只作為輸入
- [x] 5.2 三種狀態委派給呼叫端的節點
- [x] 5.3 判準：`S03`、`S12`、`S13`
- [x] 5.4 判準：容器原始碼裡 **SHALL NOT** 出現這三種狀態的文案字串
      （⚠️ 掃描前先剝掉註解 —— `FE-X13` 踩過：註解裡正好在講這件事）

## 6. 接到看板上

對應 Requirement〈走到看板前按 E，開得起對應的面板〉

- [ ] 6.1 `BoardTargets.tsx` 補 `onInteract`，既有 `id` 與 `label` 不動
- [ ] 6.2 判準：`S01`、`S02`（**成對** —— 只驗一種的話「兩塊都開案件」會全綠）
- [x] 6.3 面板走 `layer('panel')`

## 7. Escape 與世界的輸入

對應 Requirement〈Escape 關閉面板，並把世界的輸入還回去〉

- [x] 7.1 判準：`S16`、`S17`、`S18`
- [x] 7.2 ⚠️ 判準測的是**移動輸入有沒有恢復**，
      **不是**有沒有呼叫 `canvas.focus()` 或 `stopPropagation()`
- [x] 7.3 **突變**：把關閉時的還原拿掉，`S17` 要紅

## 8. 收尾

- [ ] 8.1 `npm run typecheck`、`npm run lint`、`npm test` 全綠
- [ ] 8.2 端到端只打**本機自己起的** dev server，**不連任何團隊共用位址**
- [ ] 8.3 封存（`archive/fe-b01-list-container`，獨立 PR）
