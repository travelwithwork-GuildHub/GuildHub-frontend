# `FE-A05` tasks

## 1. 選項集合

- [x] 1.1 角色選項集合從 `avatarLook()` 的值域來，**不是另外寫一份**
      （`FE-W19` 已經有 `AVATAR_COUNT`）
- [x] 1.2 ⚠️ **SHALL NOT 新增第三款角色。** 每一款都要有像素判準與 fallback 相容性

## 2. 待存值與預覽

- [x] 2.1 一個「待存的角色索引」狀態，預設是已儲存的那個
- [x] 2.2 世界裡的自己讀**待存值**，其他人讀**已儲存值**
- [x] 2.3 ⚠️ **關閉選擇器或儲存失敗時，待存值要回到已儲存值**（`S03`）
- [x] 2.4 ⚠️ **SHALL NOT 把待存值寫進送給別人的任何地方**（`S02`）

## 3. 儲存

- [x] 3.1 `PATCH /api/profiles/me` 走 `src/api/`。**元件裡不准出現 `fetch`**
- [x] 3.2 ⚠️ **`0` 是 falsy** —— 用「有值才帶欄位」的寫法會在選第一個角色時
      靜默什麼都不送，而**預覽是對的所以看起來很正常**（`S08`、design 的 `D4`）
- [x] 3.3 ⚠️ **SHALL NOT 送 `avatar_id: null`**（`S09`）
- [x] 3.4 值域外不送出（`S10`）—— 後端不驗證，存進去之後
      `FE-W19` 的 fallback 會讓它顯示成第一款

## 4. 讓別人看到

- [x] 4.1 儲存成功之後重新建立即時連線（`S04`）
- [x] 4.2 ⚠️ **儲存失敗 SHALL NOT 重連**（`S06`）
- [x] 4.3 ⚠️ **重連失敗 SHALL NOT 宣稱「其他人已經看到」**，
      但選擇仍然是已儲存的（`S07`）
- [x] 4.4 重連的入口放在 `src/realtime/`，不是在元件裡直接操作 socket

## 5. 入口

- [x] 5.1 `/world` 裡一個**一直可見**的換角色入口（`S11`）
- [x] 5.2 外觀走 `@/design/controls`（`FE-X13` 的共用定義），對比 `≥ 3:1`（`S12`）
- [x] 5.3 ⚠️ 入口要在畫布之上（`S13`）—— 堆疊層級走 `src/design/layers.ts`
- [x] 5.4 ⚠️ **SHALL NOT 做一次性提示**（design 的 `D6`）

## 6. 驗證

- [x] 6.1 V1 選取之後世界裡的自己換了外觀 —— **真 Canvas 量像素**，
      沿用 `avatar-appearance` 的保守差異（多幀交集）
- [ ] 6.2 V2 未儲存的選擇不出現在別人的畫面上
- [x] 6.3 V3 放棄之後預覽復原
- [ ] 6.4 ⚠️ V4 **兩個真的瀏覽器**：B 先在場，A 儲存之後 B 看到新外觀
- [x] 6.5 V5 儲存失敗不重連；重連失敗不宣稱生效
- [x] 6.6 V6 選第一個角色時送出的是 `avatar_id: 0`
- [x] 6.7 V7 任何路徑都不送 `null`；值域外不送出
- [x] 6.8 V8 入口不需操作就可見、有面積、對比 `≥ 3:1`、不被畫布蓋住

## 7. 突變（驗收條件）

⚠️ **不是「測試全綠」，是「把防禦拿掉，測試要變紅」。**

- [x] 7.1 X1 選取只改待存值、不改世界裡的自己 → V1 要紅
- [ ] 7.2 X2 預覽直接寫進送給別人的狀態 → V2 要紅
- [ ] 7.3 ⚠️ X3 **儲存成功後不重連 → V4 要紅、V1 要仍然綠。**
      這一條是這一列的核心，也證明 V4 不是 V1 的重複
- [x] 7.4 X4 儲存失敗也重連 → V5 要紅
- [x] 7.5 ⚠️ X5 用「有值才帶欄位」送 `PATCH` → **V6 要紅**（`0` 被吃掉）
- [x] 7.6 X6 拿掉值域檢查 → V7 要紅
- [ ] 7.7 X7 入口改成一次性提示（預設關閉）→ V8 要紅
- [ ] 7.8 X8 入口的堆疊層級拿掉 → `S13` 要紅（**只有真瀏覽器抓得到**）

## 8. 收尾

- [x] 8.1 `npm run lint`、`npm run typecheck`、`npm test` 全綠
- [x] 8.2 確認 `avatar-appearance` 既有的像素判準沒有變紅
- [x] 8.3 確認 `control-affordance` 的對比度判準沒有變紅
- [ ] 8.4 ⚠️ `docs/WBS.md` 這一列寫著「接手 `FE-A06` 的角色選擇那一段」，
      而這一輪**沒有做**（首次進入頁沒有 Canvas）—— 開 `governance/` PR 明寫
- [ ] 8.5 開一張後端票：`PATCH /api/profiles/me` 時同步 session，
      做了就能消除 `S05` 那個「離開又進來」的現象
