# tasks：`FE-A01 登入與 session`

**切法**：每一刀都帶著**直接證明它的判準**（`AGENTS.md`：拆的時候不得把實作與
證明它的判準分開）。產品碼上限 250 行／刀。

## 1. 契約與路徑判準（先做 —— 後面每一刀都靠它）

- [x] 1.1 起本機後端，重新產生 `src/api/contract/schema.d.ts`，
      更新 `GENERATED.md` 的後端 commit 與日期
      - ⚠️ **`bash run.sh` 跑不起來** —— 後端 `cd2929c` 之後它是 CRLF 換行，
        macOS 上 `set -e\r` 直接語法錯誤。改成直接叫 uvicorn，並記進 `GENERATED.md`。
        要開一張後端票。
      - **重產讓哨兵第一次真的紅了**：`_coverage`（後端多了 `RegisterIn`）
        與 `_LoginIn`（`nickname` 變選填、多三個欄位）。它在 9/8–9/10 之間
        一直是綠的，而後端那兩天走了六個 commit —— 見 5.3 那張票。
- [x] 1.2 `RequestSpec` 的 `path` 收窄成「產出型別檔中該 method 確實存在的路徑」
      —— `PathsWith<M>` ＋ 跟 `method` 綁在一起的判別式聯集
- [x] 1.3 修 `getMyProfile` 的路徑：`/api/profiles/me` → `/api/me`
- [x] 1.4 修抄錯的斷言。**不只一條** —— 同一個不存在的端點被抄了四次：
      生產碼一次、`api-operations-coverage`／`api-transport`／`api-contract-io`
      各一次。四份互相印證，所以永遠是綠的。
- [x] 1.5 `LoginIn` 契約補 `resume_token`／`login_id`／`password`
      （三選一的語意寫進註解。**不寫成 `.refine()`** —— `drift.ts` 的雙向型別
      相等會被 wrapper 打斷）。順帶把 `RegisterIn` 補進契約層與登錄表，
      但**不開對應的操作**：註冊 UI 是本 change 的 Non-goal。
- [x] 1.6 **突變測試**：跑了四次，結果如下

      | # | 突變 | 預期 | 實測 |
      |---|---|---|---|
      | 1 | `'/api/me'` → `'/api/mee'` | 紅 | ✅ `TS2820` 指名建議 `'/api/me'` |
      | 2 | `getMyProfile` 改回 `GET /api/profiles/me` | 紅 | ✅ `TS2345` |
      | 3a | `path` 放寬成 `string` ＋ 突變 1 | typecheck 變綠 | ✅ 綠（S13／S14 失效）<br>執行期測試仍紅 —— 見下 |
      | 3b | 3a 再把**測試的期望值也一起抄錯** | 全綠 | ✅ **454 條測試全綠、typecheck 綠、lint 綠**，而客戶端打的是不存在的端點 |
      | 4 | 3b 的狀態把型別約束裝回去 | 紅 | ✅ 6 個 `error TS` |

      **3a 修正了規格裡的一句話。** 規格寫「拿掉這條約束之後沒有任何其他測試
      或型別斷言會變紅」——**在今天的 repo 上這句話已經不完全成立**：
      1.4 把三個測試的期望值改對了，所以現在一個單獨改壞的路徑會被執行期
      測試抓到。真正只有型別約束抓得到的是 **3b**：生產碼與測試**抄同一個錯**。
      那正是這個 bug 實際發生的形狀，而執行期測試在結構上就抓不到它 ——
      它比對的是人抄過去的常數，型別約束比對的是後端自己宣告的事實。
      **這一條要寫進 5.1 送審的清單**（規格已在 main 上，`feat/` 分支不得回改它）。

- [x] 1.7 **送兩邊審**（提前做 5.1 的一部分）。三件事改了：

      | # | 誰 | 發現 | 處置 |
      |---|---|---|---|
      | a | Gemini | 我在 `rest.ts` 寫的「`.refine()` 會打斷 `drift.ts` 的雙向相等」**是假的** | 實測 `Equal<z.infer<Plain>, z.infer<Refined>>` 為 `true` —— 它是對的。註解改成真正的理由：**已合併的規格裡沒有任何 Scenario 要求前端擋「剛好一組」**，那是要先談定的 Requirement |
      | b | codex | `PathsWith` 需要自己的 compile-time sentinel | 採用。**壞掉的兩個方向不對稱**：太窄→每個呼叫點都紅（23 個錯誤，看得見）；太寬→**沒有任何東西會紅** |
      | c | Gemini | `PathsWith` 在「產出檔缺該 method 鍵」時會靜默漏判 | **方向對、判斷錯**：實測是 `TS2536`，打在型別定義本身。保留現在的寫法，量測結果寫進註解 |

      三條哨兵的突變測試：

      | # | 突變 | 實測 |
      |---|---|---|
      | 5 | `PathsWith` 改成恆等於 `P`（太寬鬆） | ✅ 紅，`TS2344` 打在第二條哨兵上。**同一個突變下 `api-operations-coverage` 的 17 條全綠** —— 哨兵是唯一抓到的 |
      | 6 | `PathsWith` 恆為 `never`（太窄） | ✅ 紅，23 個錯誤散在每一個呼叫點 |
      | 7 | 只留第二條哨兵，再讓 `PathsWith` 恆為 `never` | ✅ **第二條通過了** —— 證明第三條（成對那條）不能省 |

      **還沒處置的一條，兩邊獨立指到同一個地方**：`params` 是型別黑洞。
      `params?: Record<string, string>` 收任何鍵，把 `profile_id` 打成 `id` 的話
      `path.replace('{id}', …)` 找不到東西，客戶端會送出字面值
      `/api/profiles/{profile_id}`。codex 另外補了兩點：沒有驗「每個
      `{parameter}` 都有對應的 `params`」，而 `String.replace()` 只換第一個相符項。
      **這是 `S13`／`S14` 的同一個缺陷類別，但沒有任何已合併的 Requirement 涵蓋它**
      —— 要先談定，見 5.5。

## 2. 身分的取得與恢復

- [x] 2.1 `src/identity/`（PR #241、#242）
- [x] 2.2 四種狀態。**`unknown` 與 `unavailable` 都不能省** —— 前者省掉的話
      已登入的人會先看到自己是訪客再閃回名字；後者省掉的話後端一掛，
      所有人都被靜靜地登出，而畫面上跟真的沒登入一模一樣
- [x] 2.3 預設不寫。**沒選擇記住時是 `forget()` 不是「什麼都不做」** ——
      什麼都不做的話，上一個記住過的人的金鑰會留在原地，
      下一次重新載入把畫面變成**另一個人**
- [x] 2.4 判準 19 條（`tests/identity-signin.test.ts`、`tests/identity-resolve.test.ts`）
- [x] 2.5 **突變測試：11 個，全部紅**

      | 突變 | 實測 |
      |---|---|
      | 拿掉 `nicknameProblem()` 的呼叫 | 1 紅 |
      | 拿掉 `persist()` 的 `else store.forget()` | 1 紅 |
      | `remember` 的預設值改成 `true` | 2 紅 |
      | 用 `.length` 代替 `[...s].length` | 1 紅 |
      | `remember()` 變成 no-op（**完全不實作持久化**） | 1 紅 —— `S07` 的反方向 |
      | 把非 401 的失敗也當成訪客 | **3 紅**（500／連不上／契約不符各一） |
      | 401 之後不去讀金鑰 | 2 紅 |
      | 金鑰 404 時靜默改成建新名片 | 1 紅 |
      | `signInWithRecoveryKey` 的 404 不轉型別 | 1 紅 |
      | **`resolveIdentity` 快取上一次的名片** | **7 紅** —— `S04` 後兩行的理由 |
      | 401 之後重試一次 | 1 紅 |

      ⚠️ **第一個要說得更精確。** 拿掉長度檢查之後，`S02` 那條測試裡的
      **「一個網路請求都不送」那一句仍然是綠的** —— `operations.login()` 的
      Zod 契約也擋得住。紅掉的是「使用者得知的是長度不被接受」那一句。
      規格把它寫成 `AND` 而不是註腳，是對的。

## 3. 登入畫面與世界裡的身分

- [x] 3.1 `src/app/login/`（PR #243）。**這個元件不自己判斷任何規則** ——
      長度、金鑰有效性、要不要落地全部在 `src/identity/`，
      錯誤文案由錯誤的**型別**決定
- [x] 3.1b 登入入口（PR #244）。⚠️ `src/app/` 底下**沒有首頁**，
      所以「正常可到達的畫面」就是 `/world`，入口長在標題列
- [x] 3.2 `/world` 顯示目前身分（PR #244）
- [x] 3.3 判準 16 條（`tests/login-form.test.tsx`、`tests/identity-badge.test.tsx`）
- [x] 3.4 **突變測試：11 個，全部紅**

      | 突變 | 實測 |
      |---|---|
      | 失敗之後清空輸入框 | 1 紅 |
      | 金鑰只說「已經記住了」，不顯示金鑰本身 | 1 紅 |
      | 拿掉「就能成為你」那句警語 | 1 紅 |
      | 記住我預設勾起來 | 2 紅 |
      | 金鑰表單改送 `nickname` | 2 紅 |
      | **身分寫死成「訪客」** | **3 紅** —— `S12` 的對照方向抓到「兩種狀態長得一樣」 |
      | `unknown` 先顯示「訪客」 | 2 紅 |
      | `unavailable` 當成訪客 | 2 紅 |
      | 把登入入口藏起來 | 1 紅 |
      | 入口指向 `/signup`（不存在的路由） | 1 紅 |
      | 已登入也顯示「建立你的身分」 | 1 紅 |

      ⚠️ **「入口指向不存在的路由」那條**是 `S16` 的第二半：只斷言
      `href === '/login'` 的話，指向死路由的連結也會通過，而使用者按下去看到 404。

## 4. 人工驗證（判準驗不到的那一半）

⚠️ **`FE-W12` 的教訓**：444 條測試全綠，而畫面上六扇門是 10 像素的細縫。
只有人眼看得到的東西，要人眼看。

**做法改了一半**：4.1／4.3／4.4／4.4c 寫成一支端到端腳本
（`tests/e2e/identity-flow.mjs`，真的 Chromium ＋ 真的後端 ＋ 真的 Postgres），
因為它們驗的是 **cookie 有沒有真的跟著請求走**、**localStorage 在真瀏覽器裡
留不留得住**、**清掉 cookie 之後金鑰救不救得回來** —— 這三件事 jsdom 驗不到，
而它們是這個 change 的全部重點。**只有人眼看得到的那一半仍然要人眼看。**

- [x] 4.1 輸入暱稱 → 進世界 → **世界裡的名字是自己輸入的那個**（`BE-G02`
      修復的第一次可見證明）。✅ 通過。
      **而且在協定層也實測到了**：登入之後的 WS snapshot 是
      `{"name":"名字測試員","av":0,…}`，不再是「訪客」。
      `src/api/contract/ws.ts` 那段說「每個人都是訪客」的註解因此改掉了。
- [x] 4.2 **確認「看得見別人的名字」不屬於 `FE-A01`，並完成轉交。**

      ⚠️ **這個 `[x]` 的意思是「範圍歸屬已經確認並轉交」，不是「遠端名字做好了」。**

      封存前送兩邊審時 codex 一開始說不能封存，理由是這一列沒完成。
      拿到「**已合併規格的 17 條 Scenario 裡沒有任何一條要求看得見別人的名字**」
      這個事實之後**它改判了**，逐字：「4.2 就是誤放進本 change 的跨範圍驗證，
      **不能反過來擴張 `FE-A01` 的 acceptance scope**」。
      Gemini 的說法是「`tasks.md` 不能無中生有發明規格裡沒有的產品承諾」。

      最接近的兩條 Scenario 是 `S11`（世界裡顯示**自己**的名字）與 `S12`
      （看得出自己是訪客）—— 都不涉及別人。

      協定送得到 `name`（實測 snapshot 是 `{"name":"名字測試員",…}`），
      但**世界從來沒有畫過任何遠端玩家的名字**。承接的是
      `FE-R10`（Presence）為主、依賴 `FE-W08`（ProceduralAvatar）的名稱渲染能力。
      驗收敘述已經轉到那兩列（`governance/` PR）。
- [x] 4.3 重整 → 名字還在。✅ 通過。
- [x] 4.4 清掉 cookie（不清 localStorage）→ 勾過記住我的那個回得去，沒勾的變訪客。
      ✅ 兩個方向都通過，而且順帶驗了 localStorage 的實際內容。
- [x] 4.4b **入口看得見**：訪客進 `/world` 時「訪客 建立你的身分」在標題列上，
      按下去到得了 `/login`。✅ 腳本驗了可見性與導向，截圖 `1-guest-world.png`
      拿給人看。⚠️ **「一個沒看過程式碼的人找不找得到」仍然只有人眼答得出來。**
- [x] 4.4c 換裝置：全新的瀏覽器 context（無 cookie、無 localStorage），
      只貼金鑰，取回同一張名片（`S17`）。✅ 通過。
- [x] 4.5 截圖存證（`/tmp/guildhub-identity-shots/`，四張）。
      ⚠️⚠️ **截圖裡的 3D 世界是空白的，而世界沒有壞** —— 實測 three 的
      devtools hook：場景 29 個物件、8 秒 161 幀。空白是擷取的問題
      （WebGL 畫布沒開 `preserveDrawingBuffer`）。**截圖只能用來看 DOM 那一層。**

> **這一節兩次紅燈都是尺，不是產品。** 第一次 `[role="alert"]` 抓到 Next 開發
> 覆蓋層的空字串（要收成 `main [role="alert"]`），第二次是上面那個空白截圖。
> 兩次的失敗訊息都長得像產品壞掉。

## 5. 收尾

- [x] 5.1 送 codex `gpt-5.6-sol` 與 Gemini 3.1 Pro 驗證。**兩輪，三處改動。**

      **第一輪四個問題，兩邊在兩處分歧、兩處一致。**

      | | codex | Gemini |
      |---|---|---|
      | 能不能封存 | **不能**（4.2 沒完成） | 可以（邊界清楚、已排進 WBS） |
      | 「世界沒壞」的推論 | **站不住** | **站不住** |
      | 發表日最可能出的事 | 陌生人不存金鑰就永久失去名片 | 同上，而且不知道怎麼回來 |
      | 最可能做錯的決定 | `ProfileOut.id` 當恢復憑證 | `path` 收窄成字面值聯集是過度工程 |

      **一致的那條最有價值，而且我照做了。** 我原本用「場景有 29 個物件、
      8 秒 161 幀」推論世界沒壞 —— 兩邊獨立指出那只證明場景圖建起來、
      render loop 在跑。codex 逐字：「相機、材質、光源、裁切面、viewport、
      shader 或 WebGL context 都可能有問題，**同時仍有 29 個物件和 161 幀**」。
      改成直接讀 WebGL back buffer，**然後突變測試抓到我第一版判準太弱**
      （見 `tests/e2e/identity-flow.mjs` 與 PR #247）。

      **第二輪兩處分歧都收斂了，各自被一個事實推翻：**

      - **能不能封存** → codex 改判可以。推翻它的是「17 條 Scenario 裡沒有
        任何一條要求看得見別人的名字」。處置寫在 4.2。
      - **最可能做錯的決定** → 兩邊都撤回自己的答案。
        codex 撤回是因為 `resume_token = ProfileOut.id` 是**後端明訂的協定**
        （`auth.py` 逐字寫著「不必為它新增任何欄位或端點」），不是前端的決定；
        Gemini 撤回是因為「三條 sentinel 是不信任編譯器的警訊」在
        「守產生器換寫法時語意悄悄改變」這個用途下不成立 —— 它逐字說
        「TypeScript 編譯器沒有壞，但**人類抄寫字串**會壞」。

      **兩邊對發表日那條的承接點也一致：`FE-A06 首次進入`**（已標成發表前
      阻擋項目）。理由是登入畫面太早（玩家還沒體驗到價值就逼他存金鑰）、
      帳號密碼太重。判準與突變條件寫進 `FE-A06` 那一列。

      **前端現在做得到的金鑰衛生**（codex 給的清單，兩邊一致）：
      不進 URL、不進錯誤訊息、不進遙測、不當成「使用者編號」顯示、
      命名要帶憑證意涵。已經照做的部分見 `recoveryKey.ts` 的檔頭。
- [x] 5.2 開後端票：**`BE-G28` 沒有 logout 端點**（PR #246）
- [x] 5.3 開票：**`FE-O19` 契約哨兵的新鮮度**（PR #246）。
      ⚠️ **那句「它會過期而且不會有人告訴你」在這個 change 裡被證實了**：
      後端從 `27c3077` 走到 `cd2929c`（六個 commit、兩天），期間 `drift.ts`
      一路綠燈；重產之後**立刻紅了兩處**
- [x] 5.5 `params` 的型別約束：**談完了，共識是另開工作項目**（不進本 change）。

      第一輪分歧：codex 選「補進 `FE-A01` 的規格」（理由：跟 `S13`／`S14`
      是同一缺陷類別，選另開會「把 identity 建在已知不可靠的契約層上」）；
      Gemini 選「另開」（理由：9/22 只剩十幾天、107 項未開始，非阻擋性的
      範圍蔓延是致命的）。

      **第二輪 codex 改變主張**，而改變它的是一個雙方第一輪都沒拿到的事實：
      **下一刀 `src/identity/` 只用 `POST /api/login` 與 `GET /api/me`，
      這兩個都沒有路徑參數。** 有路徑參數的操作全部屬於 W3 之後。
      所以那個型別黑洞**不在下一刀的執行路徑上**。

      codex 第二輪逐字：「它仍是應修的契約缺陷，但不應重新打開 FE-A01、
      阻塞 identity；應獨立追蹤，並**在 W3 首個帶路徑參數的功能開始前完成**。」
      —— 那個時間點就是這張票的期限，要寫進工作項目（見 5.4）。

- [x] 5.5b `path.replace()` 只換第一個相符項：**兩邊都說現在不修。**
      目前 16 個端點沒有任何一條路徑出現同一個 placeholder 兩次，
      改成全域替換是在替不存在的呼叫端設計行為。
      差別只在要不要留 TODO：codex 說連 TODO 都不要（「TODO 只會把猜測永久化」），
      Gemini 說留一行。**採 codex** —— 這個 repo 的 `docs/DECISIONS.md`
      本來就是「拒絕過什麼」的存放處，猜測不該散在程式碼裡。這一條記在這裡。

- [x] 5.5c **發表日的範圍裁決材料**：兩個審查者對「現在最該做什麼」的答案
      **完全一致** —— 不是 params、不是 identity、是範圍裁決。
      那是專案負責人的決定，我能做的是把材料備好。兩邊給的欄位收斂成：
      ID＋名稱 / **剩餘成本**（狀態＋點數）/ **不做的話發表日失去什麼**
      ＋時序與相依。codex 特別指出第二欄不能只寫「阻塞原因」，
      要寫「若不做，發表日會失去什麼」，否則負責人得重新理解整份 WBS。

      **算出來的數字**：W1–W5 前端 72 項 641 點，做掉 226（35%）；
      到 9/22 是 12 天，實績速度 22.6 點／天 ⇒ 產能約 271 點；剩 415 點。
      最大的兩塊是「本地後端 `FE-O03`／`O04`／`O05` 共 57 點」與
      「W4 專案空間 62 點」。

      ⚠️ **裁決本身被專案負責人否決了：不砍範圍，W1–W5 這禮拜做完、
      9/22 之前做完 W12。** 材料留著當參考，不當提案。
- [x] 5.6 開後端票：**`BE-G29` `run.sh` 是 CRLF 換行**（PR #246）。
      三個症狀（`set: -: invalid option` → `cd: .: No such file` → `syntax error`）
      **沒有一個指向換行符號**
- [x] 5.4 `governance/` PR（#246 與 #249）：
      - [x] `FE-A06` 標成**發表前阻擋項目**，並多承接「陌生人不存金鑰」那條風險
      - [x] `FE-R10`／`FE-J14` 標成小玉負責（PR #239，本 change 之前）
      - [x] `FE-A08` 帳號密碼登入與註冊（design D1）
      - [x] `FE-O20` `params` 的型別約束，**期限是 W3 第一個帶路徑參數的功能之前**
      - [x] `BE-G30` `resume_token` 就是 `ProfileOut.id` 的安全債
      - [x] 三處轉交：`FE-W08` 接名字渲染、`FE-R10` 接雙瀏覽器互見的驗收，
            依賴寫進〈跨項依賴〉（**寫在阻塞欄會紅** —— 實測過）
