# tasks：`FE-A06 首次進入`

**切法**：每一刀都帶著直接證明它的判準。產品碼上限 250 行／刀。

## 1. 流程本身（`S07`–`S10`：金鑰要真的被帶走）

- [ ] 1.1 剪貼簿的存取包成一個可注入的介面。**注入的是「寫入會成功／會失敗」，
      不是一個 `toHaveBeenCalledWith` 的靶子**（design D4）
- [ ] 1.2 首次進入流程的元件：取名字 → 建立身分 → 顯示金鑰 →
      「進入世界」**預設不可用**
- [ ] 1.3 兩條放行路徑：複製成功、或明確勾選「我已經自己保存了」
- [ ] 1.4 判準：`S07`／`S08`／`S09`／`S10`
- [ ] 1.5 **突變測試**
      - [ ] 「進入世界」預設就可用 → `S07` 必須紅
      - [ ] 在點擊處理函式裡直接把狀態設成已複製（不等寫入結果）→ `S09` 必須紅
      - [ ] 寫入失敗時也顯示成功 → `S09` 必須紅
      - [ ] 拿掉勾選那條路 → `S10` 必須紅，**而且要確認 `S08` 仍然綠**
            （證明兩條路是獨立的）

## 2. 兩個入口（`S01`–`S06`）

- [ ] 2.1 `/` 從轉址改成正式入口。**`next.config.ts` 那條 307 要拿掉** ——
      它從第一天就是暫時的
- [ ] 2.2 `/world` 對訪客顯示可關掉的提示，通往同一個流程
- [ ] 2.3 「看過引導了沒有」與「完成首次進入了沒有」**是兩件事**（`S06`）
- [ ] 2.4 判準：`S01`–`S06`
- [ ] 2.5 **突變測試**
      - [ ] 關掉提示就記成完成 → `S06` 的後兩行必須紅
      - [ ] 提示用一個吃掉指標事件的全螢幕遮罩 → `S05` 必須紅
      - [ ] `/` 對已經有身分的人也顯示流程 → `S03` 必須紅
      - [ ] 永遠顯示提示 → `S06` 的最後兩行必須紅

## 3. 剪貼簿的真相（`S11`）

- [ ] 3.1 E2E：授予 `clipboard-read`／`clipboard-write` 的 Chromium，
      按下複製之後**由測試自己讀回剪貼簿**，斷言跟畫面上的金鑰逐字相同。
      **MUST NOT mock 任何剪貼簿 API**
- [ ] 3.2 走完整條路：`/` → 取名字 → 複製 → 進世界 → 世界裡是那個名字

## 4. 人工驗證

⚠️ **`FE-W12` 的教訓**：444 條測試全綠，而畫面上六扇門是 10 像素的細縫。

- [ ] 4.1 用發表版的實際 viewport 走完一次首次進入。
      **金鑰、警語、複製按鈕、成功／失敗回饋要同時看得見**
      （codex：「不能只靠 DOM 存在、文字查詢或元件 snapshot 判定完成」）
- [ ] 4.2 找一個沒看過這個專案的人走一次，**不給任何提示** ——
      看他有沒有把金鑰帶走
- [ ] 4.3 截圖存證

## 5. 收尾

- [ ] 5.1 送 codex `gpt-5.6-sol` 與 Gemini 3.1 Pro 驗證
- [ ] 5.2 `governance/` PR：把 `FE-A06` 的「角色選擇」整段移到 `FE-A05`，
      並在 `FE-A05` 上寫明它自己的前置條件（**世界要先畫得出不同的外觀**，
      否則選擇器是假的 —— design D2）
- [ ] 5.3 `governance/` PR：新增一個工作項目 **「`ChibiPlayer` 依 `av` 呈現差異」**
      —— codex 指出的最小可見閉環，不必等完整的 `FE-W08`
