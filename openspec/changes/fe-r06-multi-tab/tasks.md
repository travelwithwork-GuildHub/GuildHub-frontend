## 1. 規格

- [ ] 1.1 規格已在 PR 上談定（`spec/fe-r06-multi-tab` 合併進 `main`）。
      驗證：`npx --no-install openspec validate fe-r06-multi-tab --strict` 通過且 PR 已合併

## 2. 匿名多分頁不得被擋（今天就可以驗）

- [ ] 2.1 `FE-R06-S01`：瀏覽器 E2E 開兩個分頁，兩邊都連得上、
      而且在對方名單裡各是一個獨立的人。
      驗證：`tests/e2e/interpolation-smoothness.mjs` 已經在做這件事 ——
      把它算成這條的證據，並在腳本裡指名 Scenario ID

## 3. 登入之後的守衛（**擋著，不要現在做**）

> ⚠️ **前提：登入存在，而且 `hello.you` 對同一個人是穩定的。**
> 今天匿名連線每條一個新的 `uuid4`，協調沒有可用的鍵。
> **在錯誤的鍵上實作守衛比沒有守衛更糟** —— 它會擋掉合法的匿名多分頁
> （那是我們本機驗證多人行為的方式）。
>
> 阻塞於：`BE-G01`（身分模型，`待裁決`）與登入的工作項目。

- [ ] 3.1 `FE-R06-S02`：第二個分頁不連線，顯示提示與「改用這個分頁」
- [ ] 3.2 `FE-R06-S03`：第一個分頁關掉之後第二個接手，中間不得兩條同時存在
- [ ] 3.3 `FE-R06-S04`：跨裝置不在射程內 —— 這一條是**文件性的**，
      驗證方式是規格裡有寫，不是測試

## 4. 後端的票（不是這個 repo 的工作）

- [ ] 4.1 開一張後端票：`presence.join()` 對既有的 `user_id` 不得重建 `Player`；
      連線數與玩家狀態分開（`disconnect` 已經有 `still_here`，`join` 沒有對應的那一半）
