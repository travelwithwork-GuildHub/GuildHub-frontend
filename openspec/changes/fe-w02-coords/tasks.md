## 1. 規格

- [x] 1.1 規格已在 PR 上談定：`spec/fe-w02-coords` 合併進 `main`；驗證：`git log --oneline main -- openspec/changes/fe-w02-coords/proposal.md` 有輸出

## 2. 雙向對映（Requirement: 世界座標與協定像素的雙向對映）

- [x] 2.1 寫 `src/world/coords.ts`：世界座標 ↔ 協定像素，比例 32，原點重合，世界 Z 對映協定 `y`；驗證：`(1, 2)` → `x=32, y=64`（Scenario `FE-W02-S01`）
- [x] 2.2 取整規則固定為 `.5` 往正無窮，**正負兩側都要驗**；驗證：`+0.015625` → `1` 且 `-0.015625` → `0`（不是 `-1`）。只驗正側的話，一個對稱進位的實作照樣是綠的（Scenario `FE-W02-S02`）
- [x] 2.3 協定像素轉回世界座標；驗證：`(32, 64)` → `(1, 2)`（Scenario `FE-W02-S03`）
- [x] 2.4 **負向驗證**：把 `Math.round` 改成 `Math.floor`，`S02` 要從綠變紅；把比例改掉，`S01` 要從綠變紅；驗證：記錄兩次的退出碼

## 3. 往返穩定（Requirement: 往返不得累積誤差）

- [x] 3.1 對一組含負數與零的協定像素值做往返，斷言每個值不變；驗證：Scenario `FE-W02-S04`
- [x] 3.2 同一個世界座標連續轉換 100 次得到相同結果；驗證：Scenario `FE-W02-S05`
- [x] 3.3 在程式碼註解寫明**量化結果不得寫回 3D 位置**，並指出症狀（走一分鐘之後位置歪掉且沒有錯誤訊息）；驗證：註解存在。**這一條沒有 Scenario** —— 純函式管不到呼叫端怎麼用它的回傳值，只有 review 擋得住
- [x] 3.4 在 `coords.ts` 的註解指名 **`FE-W03` 要驗「按下往下的鍵，角色在畫面上往下走」** —— 那是本項證明不了的視覺對映（design 的 R4）；驗證：註解存在

## 4. 明顯失敗（Requirement: 不合協定的值必須明顯失敗）

- [x] 4.1 `NaN` / `Infinity` / `-Infinity` 輸入要拋錯，不得回傳數值；驗證：Scenario `FE-W02-S06`
- [x] 4.2 結果超出安全整數範圍要拋錯；驗證：Scenario `FE-W02-S07`
- [x] 4.3 **負向驗證**：拿掉非有限值的檢查，`S06` 要從綠變紅；驗證：記錄兩次的退出碼

## 5. 朝向（Requirement: 移動方向對映到離散朝向）

- [x] 5.1 四個主方向對映到 `0`/`1`/`2`/`3`，編碼對齊 `protocol.py`；驗證：Scenario `FE-W02-S08`
- [x] 5.2 對角線取縱向，規則固定；驗證：Scenario `FE-W02-S09`
- [x] 5.3 零向量回 `null`，不是任選一個朝向；驗證：Scenario `FE-W02-S10`
- [x] 5.5 方向向量含 `NaN`／`Infinity` 要拋錯，**不得回報「沒有方向」**；驗證：Scenario `FE-W02-S11`。把壞掉的輸入算成「站著不動」等於把 bug 偽裝成正常狀態
- [x] 5.4 **負向驗證**：把零向量的回傳改成 `0`，`S10` 要從綠變紅；驗證：記錄兩次的退出碼

## 6. 完成前的驗收

- [x] 6.1 交出 Scenario ID ↔ 測試的對照表，`FE-W02-S01`–`S10` 每一條都指得出對應的測試；驗證：表格中沒有空格
- [x] 6.2 貼出 `npm run lint` / `typecheck` / `test` / `build` 四個指令的實際輸出
- [x] 6.3 確認**正式碼裡沒有任何地方複製了比例常數**：`grep -rn "32" src/` 的命中只能在 `coords.ts` 的那一行定義（測試檔會出現 `32` 是正常的 —— 斷言本來就要寫出期望值，所以只掃 `src/`）；驗證：貼出 grep 結果
- [x] 6.4 `npx openspec validate fe-w02-coords --strict` 通過，且本檔案沒有殘留的未完成項
