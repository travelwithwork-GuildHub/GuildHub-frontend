## 1. 規格

- [ ] 1.1 規格已在 PR 上談定：`spec/fe-w02-coords` 合併進 `main`；驗證：`git log --oneline main -- openspec/changes/fe-w02-coords/proposal.md` 有輸出

## 2. 雙向對映（Requirement: 世界座標與協定像素的雙向對映）

- [ ] 2.1 寫 `src/world/coords.ts`：世界座標 ↔ 協定像素，比例 32，原點重合，世界 Z 對映協定 `y`；驗證：`(1, 2)` → `x=32, y=64`（Scenario `FE-W02-S01`）
- [ ] 2.2 取整規則固定為 `.5` 往正無窮；驗證：`0.015625` 個世界單位 → `1`（Scenario `FE-W02-S02`）
- [ ] 2.3 協定像素轉回世界座標；驗證：`(32, 64)` → `(1, 2)`（Scenario `FE-W02-S03`）
- [ ] 2.4 **負向驗證**：把 `Math.round` 改成 `Math.floor`，`S02` 要從綠變紅；把比例改掉，`S01` 要從綠變紅；驗證：記錄兩次的退出碼

## 3. 往返穩定（Requirement: 往返不得累積誤差）

- [ ] 3.1 對一組含負數與零的協定像素值做往返，斷言每個值不變；驗證：Scenario `FE-W02-S04`
- [ ] 3.2 同一個世界座標連續轉換 100 次得到相同結果；驗證：Scenario `FE-W02-S05`
- [ ] 3.3 在程式碼註解寫明**量化結果不得寫回 3D 位置**，並指出症狀（走一分鐘之後位置歪掉且沒有錯誤訊息）；驗證：註解存在

## 4. 明顯失敗（Requirement: 不合協定的值必須明顯失敗）

- [ ] 4.1 `NaN` / `Infinity` / `-Infinity` 輸入要拋錯，不得回傳數值；驗證：Scenario `FE-W02-S06`
- [ ] 4.2 結果超出安全整數範圍要拋錯；驗證：Scenario `FE-W02-S07`
- [ ] 4.3 **負向驗證**：拿掉非有限值的檢查，`S06` 要從綠變紅；驗證：記錄兩次的退出碼

## 5. 朝向（Requirement: 移動方向對映到離散朝向）

- [ ] 5.1 四個主方向對映到 `0`/`1`/`2`/`3`，編碼對齊 `protocol.py`；驗證：Scenario `FE-W02-S08`
- [ ] 5.2 對角線取縱向，規則固定；驗證：Scenario `FE-W02-S09`
- [ ] 5.3 零向量回 `null`，不是任選一個朝向；驗證：Scenario `FE-W02-S10`
- [ ] 5.4 **負向驗證**：把零向量的回傳改成 `0`，`S10` 要從綠變紅；驗證：記錄兩次的退出碼

## 6. 完成前的驗收

- [ ] 6.1 交出 Scenario ID ↔ 測試的對照表，`FE-W02-S01`–`S10` 每一條都指得出對應的測試；驗證：表格中沒有空格
- [ ] 6.2 貼出 `npm run lint` / `typecheck` / `test` / `build` 四個指令的實際輸出
- [ ] 6.3 確認**沒有任何地方複製了比例常數** —— `grep -rn "32" src/` 只在 `coords.ts` 命中那個定義；驗證：貼出 grep 結果
- [ ] 6.4 `npx openspec validate fe-w02-coords --strict` 通過，且本檔案沒有殘留的未完成項
