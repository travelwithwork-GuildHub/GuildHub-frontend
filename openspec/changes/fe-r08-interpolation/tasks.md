## 1. 規格

- [x] 1.1 規格已在 PR 上談定（`spec/fe-r08-interpolation` 合併進 `main`），
      驗證：`openspec validate fe-r08-interpolation --strict` 通過且 PR 已合併

## 2. 插值本身（純函式，不碰 React／three／WebSocket）

對應 `remote-interpolation` 的全部 Requirement。

- [x] 2.1 樣本的型別與緩衝：`{ x, z, f, t }`，以及「追加一個樣本」。
      驗證：追加之後緩衝裡有那一筆，時間是傳進去的 `now` 不是內部取的
- [x] 2.2 單段時長上限＝`RENDER_DELAY`：追加時把前一個樣本的時間往前推到
      最多 `RENDER_DELAY` 之前。驗證：**FE-R08-S04**（靜止 10 秒後重新移動不跳格）
- [x] 2.3 求值：`(緩衝, 游標) → { x, z, f }`，線性內插。
      驗證：**FE-R08-S01**（連續前進、同一游標求值兩次相同）
- [x] 2.4 夾住：游標超過最後一個樣本就停在它上面，不外插。
      驗證：**FE-R08-S03**（推進 10 秒位置完全不變）
- [x] 2.5 時長為零的段不得產生 `NaN`。
      驗證：**FE-R08-S05**（一批時間相同的樣本）
- [x] 2.6 朝向取這一段終點的 `f`，夾住時取最後一個樣本的 `f`。
      驗證：**FE-R08-S08**、**FE-R08-S09**（原地轉身）
- [x] 2.7 清掉游標已經越過的舊樣本。
      驗證：**FE-R08-S16**（一萬則之後樣本數有上界，且結果與不清理時相同）
- [x] 2.9 游標早於第一個樣本時夾在第一個樣本上。驗證：**FE-R08-S14**
- [x] 2.10 時間倒退時把樣本時間夾成單調。驗證：**FE-R08-S15**
- [x] 2.11 游標就是 `now − 250`，而且單段上限與它相等。驗證：**FE-R08-S13**
- [x] 2.8 10 Hz 樣本在 60 FPS 上的跳格判準。
      驗證：**FE-R08-S02**（最大單幀位移不超過中位數兩倍）

## 3. 接進遠端玩家

對應 `remote-players` 的 MODIFIED Requirement。

- [x] 3.1 `applyMessage` 收 `now`，`pos` 改成追加樣本。
      驗證：**FE-R07-S06**、**FE-R08-S10**（第二則到達時畫面還在第一則附近）
- [x] 3.2 `snapshot` 與 `presence.join` 清空樣本並直接就位 ——
      **包含「仍然在名單裡」與「沒有先 leave」這兩種**。
      驗證：**FE-R08-S06**、**FE-R08-S18**、**FE-R08-S17**、**FE-R07-S02**、**FE-R07-S03**
- [x] 3.3 `leave` 把樣本一起清掉。
      驗證：**FE-R08-S11**（**直接斷言容器**，不是斷言重新 join 之後的畫面）
- [x] 3.4 距離很遠的 `pos` **仍然插值**，沒有距離門檻。
      驗證：**FE-R08-S07**
- [x] 3.5 render loop 改成求值。時間來源注入，寫入端與求值端同一個。
      驗證：**FE-R07-S07**、**FE-R07-S08**、**FE-R08-S19**（假時鐘推進畫面就會動）
- [x] 3.6 樣本被就地刪掉、但名單還沒重繪的那一幀不拋錯、也不寫入 transform。
      驗證：**FE-R08-S20**（**只改 Map、不動名單** —— 把角色卸載掉來測的話，
      它的 `useFrame` 根本不會再被呼叫，守衛整條拿掉照樣是綠的）

## 4. 高頻資料不進 React

- [x] 4.1 插值的每一幀都改變 transform，而且都不觸發 React 重繪。
      驗證：**FE-R08-S12**（兩個斷言要在同一條裡 —— 只驗「沒重繪」的話，
      render loop 什麼都不做也會綠）

## 5. 瀏覽器 E2E（design.md 的 V2）

- [ ] 5.1 加 `playwright-core` 到 devDependencies（**不下載瀏覽器**，
      用系統既有的 Chromium）。驗證：`npm ci` 之後腳本跑得起來
- [ ] 5.2 兩個分頁連到**本機自己起的後端**，一邊走、另一邊逐幀取樣遠端角色的座標。
      驗證：腳本印出每幀位移的中位數與 p99
- [ ] 5.3 三條判準（見 design.md〈驗證方式〉V2）：零位移幀 < 5%、
      每幀位移中位數 > 0、**速度**的 p99 ÷ 中位數 ≤ 3。驗證：跑出來的數字貼進 PR
- [ ] 5.4 這支腳本自己也要能紅：把插值拿掉（改成直接用最後一個樣本）跑一次，
      三條都要紅。驗證：兩次執行的輸出都貼進 PR

## 6. 負向驗證（design.md 的 V3）

**驗收條件不是「測試全綠」，是「把防禦拿掉，測試要變紅」。**

- [x] 6.1 拿掉單段時長上限 → **FE-R08-S04** 要紅
- [x] 6.2 把朝向改成用這一段起點的 `f` → **FE-R08-S08** 要紅
- [x] 6.3 把求值改回「直接用最後一個樣本」（等於沒有插值）→
      **FE-R08-S02** 要紅，而且 5.3 的零位移幀比例會從約 0% 跳到約 83%
- [x] 6.4 把時間來源改成求值端自己取 `performance.now()`（而不是注入的）→
      **FE-R08-S19** 要紅
- [x] 6.5 把單段上限改成 200 或 300（有 cap 但不等於 delay）→ **FE-R08-S13** 要紅
- [x] 6.6 把 `leave` 的樣本清理拿掉 → **FE-R08-S11** 要紅
      （用「重新 join」來測的話這裡會是綠的，那就是測試寫錯了）
- [x] 6.7 把 render loop 改成什麼都不做 → **FE-R08-S12** 要紅
- [x] 6.8 把 render loop 裡「樣本不見了就不寫」那個守衛拿掉 → **FE-R08-S20** 要紅
      （會在 `evaluate(undefined, …)` 拋 `TypeError`）
- [x] 6.9 把 `snapshot` 對既有的人改成追加樣本而不是重建 → **FE-R08-S18** 要紅
