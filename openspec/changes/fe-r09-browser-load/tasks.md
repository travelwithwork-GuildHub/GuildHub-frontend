## 1. 規格

- [x] 1.1 規格已在 PR 上談定（`spec/fe-r09-browser-load` 合併進 `main`）。
      驗證：`npx --no-install openspec validate fe-r09-browser-load --strict` 通過且 PR 已合併

## 2. 假 client（`FE-R09-S03`）

- [x] 2.1 WebSocket client。**Q4 的答案：Node 24 內建的 `WebSocket`**，
      不多一個相依。實測 40 條同時連線全部連得上、零斷線
- [x] 2.2 抖動、同幀突發、移動變化 —— **參數可調，而且記錄在輸出裡**
- [x] 2.3 `FE-R09-S09`：連不滿預期人數時失敗並回報實際連上幾個。
      **負向**：故意讓後端只接受一半 → 必須失敗而不是用比較少的人數繼續量

## 3. 網路基準（`FE-R09-S05`、`S06`）

- [x] 3.1 純 WebSocket 的基準，**不開任何瀏覽器**。回報到達間隔分佈與掉線數
- [x] 3.2 `FE-R09-S06`：全部靜止時 `pos` 數為 0。
      **這同時是工具的健康檢查** —— 收到封包代表工具或後端跟預期不符
- [ ] 3.3 對照後端自己的 `tools/run_swarm.py --n 40 --seconds 300`。
      **還沒做** —— 那是後端 repo 的工具，要在那邊跑

## 4. 渲染預算（`FE-R09-S01`、`S02`）

- [x] 4.1 一個真實瀏覽器連上，用 `__THREE_DEVTOOLS__` 取 Scene，
      數畫面上實際有幾個遠端角色
- [x] 4.2 `FE-R09-S02`：人數對不上時**失敗，不回報 FPS**。
      **負向**：故意少連一個 client → 必須失敗
- [x] 4.3 **Q3 的答案：用 rAF 計數**，不用 CDP 的 metrics —— 後者給的是整個
      renderer process 的統計，而這裡要的是「這個頁面的 render loop 跑了幾次」。
      **Q2：先看分佈，還沒訂判準** —— 見 7.1
- [x] 4.4 Q1：爬升量測（SwiftShader 軟體渲染，沒有 GPU）

          N     FPS    幀間隔中位數   p99
          0    41.4      16.7 ms     33.4
          5    35.8      33.3        50
          20   29.9      33.3        66.6
          39   24.4      33.4        66.8

      **沒有懸崖，是平緩下降。** 假 client 那一邊 40 個全部連得上、零斷線 ——
      工具的上限還沒碰到

## 5. 誠實的輸出（`FE-R09-S07`、`S08`）

- [x] 5.1 每一次輸出都帶「這把尺量不到什麼」那段（失敗時也印）
- [x] 5.2 `FE-R09-S08`：後端沒起來時失敗並指出是哪一環。
      **負向**：把後端關掉跑一次，貼失敗訊息

## 6. 負向驗證（**這一節是驗收**）

- [x] 6.1 `FE-R09-S04`：N=39 各跑兩次

                    FPS #1   FPS #2   p99 #1   p99 #2
          有抖動      27.5     23.3     66.6     **133.4**
          乾淨        31.0     30.6     66.7     50.1

      **乾淨的一致地比較好。** 一個送得太漂亮的假 client 會把 FPS 高報約 20%，
      而且完全看不到那個 133 ms 的長幀
- [x] 6.2 只連 19 個人卻宣稱 39 個：

          檢查在：❌ 畫面上只有 19 個遠端角色，預期 39 個（FE-R09-S02）
          檢查拿掉：RESULT {"n":39,"remotes":19,"fps":29.75}

      **29.75 比真正 39 人的 23–27 還好看** —— 那個數字會被當成「39 人沒問題」
- [ ] 6.3 拿掉輸出裡的限制說明 → `S07` 必須紅

## 7. 判定與文件

- [ ] 7.1 把量到的數字寫成一份判定：**這台機器上，一個瀏覽器撐得住幾個遠端角色**
- [ ] 7.2 `docs/WBS.md` 的 `FE-R09` 改成這個拆法，並記下硬體的理由（另開 `chore/`）
- [ ] 7.3 `FE-W13`（W5）的數字目標從這裡來 —— 在那一項的欄位裡指回這份判定
