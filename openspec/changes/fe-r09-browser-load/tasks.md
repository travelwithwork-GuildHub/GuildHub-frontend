## 1. 規格

- [ ] 1.1 規格已在 PR 上談定（`spec/fe-r09-browser-load` 合併進 `main`）。
      驗證：`npx --no-install openspec validate fe-r09-browser-load --strict` 通過且 PR 已合併

## 2. 假 client（`FE-R09-S03`）

- [ ] 2.1 WebSocket client：連上自己起的後端、走路、送 `move`。
      回答 Q4（用什麼寫、撐得住幾條）
- [ ] 2.2 抖動、同幀突發、移動變化 —— **參數可調，而且記錄在輸出裡**
- [ ] 2.3 `FE-R09-S09`：連不滿預期人數時失敗並回報實際連上幾個。
      **負向**：故意讓後端只接受一半 → 必須失敗而不是用比較少的人數繼續量

## 3. 網路基準（`FE-R09-S05`、`S06`）

- [ ] 3.1 純 WebSocket 的基準，**不開任何瀏覽器**。回報到達間隔分佈與掉線數
- [ ] 3.2 `FE-R09-S06`：全部靜止時 `pos` 數為 0。
      **這同時是工具的健康檢查** —— 收到封包代表工具或後端跟預期不符
- [ ] 3.3 對照後端自己的 `tools/run_swarm.py --n 40 --seconds 300`，
      兩邊的數字要能互相解釋。對不上的話**先查工具再查產品**

## 4. 渲染預算（`FE-R09-S01`、`S02`）

- [ ] 4.1 一個真實瀏覽器連上，用 `__THREE_DEVTOOLS__` 取 Scene，
      數畫面上實際有幾個遠端角色
- [ ] 4.2 `FE-R09-S02`：人數對不上時**失敗，不回報 FPS**。
      **負向**：故意少連一個 client → 必須失敗
- [ ] 4.3 FPS 的取得方式（回答 Q3），並先**看分佈再決定判準**（Q2）——
      不要先訂一個數字再去湊
- [ ] 4.4 回答 Q1：從小往上爬，記錄「被測瀏覽器開始掉幀的人數」與
      「假 client 開始連不滿的人數」。**兩個要分開記**

## 5. 誠實的輸出（`FE-R09-S07`、`S08`）

- [ ] 5.1 每一次輸出都帶「這把尺量不到什麼」那段
- [ ] 5.2 `FE-R09-S08`：後端沒起來時失敗並指出是哪一環。
      **負向**：把後端關掉跑一次，貼失敗訊息

## 6. 負向驗證（**這一節是驗收**）

- [ ] 6.1 `FE-R09-S04`：關掉抖動與突發跑一次，數字必須變好。
      **兩次一樣的話，代表那些特性沒有進到被測瀏覽器**
- [ ] 6.2 拿掉 `S02` 的人數檢查 → 少連一半的情況會回報一個漂亮的 FPS（貼證據）
- [ ] 6.3 拿掉輸出裡的限制說明 → `S07` 必須紅

## 7. 判定與文件

- [ ] 7.1 把量到的數字寫成一份判定：**這台機器上，一個瀏覽器撐得住幾個遠端角色**
- [ ] 7.2 `docs/WBS.md` 的 `FE-R09` 改成這個拆法，並記下硬體的理由（另開 `chore/`）
- [ ] 7.3 `FE-W13`（W5）的數字目標從這裡來 —— 在那一項的欄位裡指回這份判定
