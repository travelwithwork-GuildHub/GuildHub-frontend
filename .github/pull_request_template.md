## 這個 PR 對應哪個 change

`openspec/changes/<change-name>/`

## 目前狀態

- [ ] **規格階段** —— 只有 `openspec/`，還沒有實作。
      請先看 `openspec/changes/<change>/specs/` 底下的 Requirement 與 Scenario。
- [ ] **實作階段** —— 規格已談定，以下是實作。

## 規格談定了嗎

<如果是實作階段：規格在哪一則討論上談定的？改過什麼？>

## 驗證輸出

<貼實際指令輸出，不要只寫「測試通過」>

```
$ 
```

## 有沒有偏離規格

<沒有就寫「沒有」。有的話說明為什麼，以及規格改了沒。>
